#include "day2.h"
#include "common.h"
#include <unordered_map>

const std::string filename = "../testinput/day2.txt";

void day2part1() {
    std::ifstream input = open_file(filename);

    for (std::string line; getline(input, line); ) {

    }
}

void day2part2() {
    return; 
}
